# ---------------------------------------------------------------------------
# Core backend — required. The app starts and the full demo pipeline works
# with just this section installed (MOCK_INFERENCE=true by default).
# ---------------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
numpy>=1.26.0
Pillow>=10.3.0

# ---------------------------------------------------------------------------
# AI/ML — only required once MOCK_INFERENCE=false and a real Hugging Face
# VLM backend is wired in (app/models/vlm.py::HFVLMBackend). Heavy; install
# separately when moving past the mock pipeline.
# ---------------------------------------------------------------------------
torch>=2.2.0
transformers>=4.42.0
datasets>=2.20.0
peft>=0.11.0
torchvision>=0.17.0
opencv-python-headless>=4.10.0

# ---------------------------------------------------------------------------
# Remote sensing / geospatial — only required for real GeoTIFF reading,
# reprojection and CRS-aware alignment (app/geospatial/*). The app degrades
# gracefully to Pillow-based reads when these aren't installed, which is
# useful for a lean CPU-only demo environment. GDAL in particular often
# needs a matching system package (`apt install gdal-bin libgdal-dev`)
# before `pip install gdal` will succeed — see README.
# ---------------------------------------------------------------------------
rasterio>=1.3.0
# gdal>=3.8.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
scikit-learn>=1.5.0

# ---------------------------------------------------------------------------
# Optional: true PDF report generation (falls back to HTML reports if
# unavailable — see app/services/report_generator.py)
# ---------------------------------------------------------------------------
# weasyprint>=62.0

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=8.2.0
httpx>=0.27.0
